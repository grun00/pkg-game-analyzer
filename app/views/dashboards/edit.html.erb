<% content_for :title, "Edit Dashboard – TRAINER_OS" %>

<div class="page-hd">
  <div>
    <h1 class="page-title">Reconfigure Dashboard</h1>
    <p class="page-sub">rename "<%= @dashboard.name %>"</p>
  </div>
</div>

<%= render "form", dashboard: @dashboard %>

<div style="margin-top:24px;">
  <%= link_to "← BACK TO DASHBOARD", dashboard_path(@dashboard), class: "btn-ghost c-dim" %>
</div>
