<% content_for :title, "Register Match – TRAINER_OS" %>

<div class="page-hd">
  <div>
    <p class="breadcrumb"><%= link_to "← #{@dashboard.name}", dashboard_path(@dashboard) %></p>
    <h1 class="page-title">Log Match</h1>
    <p class="page-sub">record the details of your latest battle</p>
  </div>
</div>

<%= render "form", match: @match, dashboard: @dashboard %>

<div style="margin-top:24px;">
  <%= link_to "← BACK TO DASHBOARD", dashboard_path(@dashboard), class: "btn-ghost c-dim" %>
</div>
